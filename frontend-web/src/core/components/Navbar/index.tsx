import React from 'react';
import './styles.scss'

const Navbar = () => (
    <nav className="nav bg-primary main-nav">
        <h1 className="main-nav-text">MovieFlix</h1>
    </nav>
);

export default Navbar;